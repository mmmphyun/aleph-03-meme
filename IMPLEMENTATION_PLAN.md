# [과제 3. 짤·카드 스튜디오] 나만의 세레모니 제조기 구현 계획서

## 1. 프로젝트 개요
* **프로젝트명**: 축구장 세레모니 제조기 (Ceremony Maker)
* **목적**: 축구장 배경 위에 불쾌한 골짜기 감성의 마네킹 3D 모델을 배치하고, 얼굴 사진 크롭 합성, 대두 모드, 최신 챌린지 포즈(거제 야호, 최산 BAD 등)를 조합하여 세 가지 비율(1:1, 4:5, 9:16)의 짤/카드를 제작하고 다운로드함.
* **배포 환경**: GitHub Pages (정적 호스팅, T03-C01 무로그인/무인증 접근 보장)
* **핵심 기준**: 과제 3 통과 기준 T03-C01 ~ T03-C32 100% 충족

---

## 2. 기술 스택 및 아키텍처

### 2.1. 기술 스택
* **코어**: HTML5, CSS3, Modern JavaScript (ES Modules, 번들러 없음)
* **3D 렌더링**: Three.js (r128+, CDN ES Module import) + OrbitControls
* **얼굴 크롭 & 텍스처**: 2D Canvas API (`drawImage` 기반 가이드 틀 크롭 -> `THREE.CanvasTexture`)
* **저장소**: Browser `localStorage` (템플릿 영속성)

### 2.2. 디렉터리 구조
```text
c:\work\aleph-03-meme/
├── index.html                    # 단일 페이지 메인 UI (3D 뷰포트 + 컨트롤 툴바)
├── styles/
│   └── main.css                  # 축구장 뷰포트 및 미니멀 툴바 스타일
├── scripts/
│   ├── app.js                    # UI 이벤트 바인딩 및 상태 총괄
│   ├── scene-manager.js          # Three.js 씬, 축구장 배경, 조명, 렌더러
│   ├── character-model.js        # 마네킹 메쉬, 대두 슬라이더, 포즈 프리셋
│   ├── face-cropper.js           # 원형/타원형 얼굴 사진 크롭 모달 및 텍스처 생성
│   ├── template-store.js         # localStorage 템플릿 CRUD (3개 이상)
│   └── json-validator.js         # JSON 가져오기 3단계 검증 (정상/문법손상/누락)
├── assets/
│   └── samples/                  # 완성 이미지 3개 및 기본 샘플 얼굴/텍스처
├── docs/
│   ├── test-cases.md             # 극단 입력 12건 검사표 및 결함 개선 기록
│   └── submission.md             # 제출 규격 (4줄 확인법, 3줄 AI 판단)
├── TASK_03_GUIDE.md              # 요구사항 명세 원본
└── IMPLEMENTATION_PLAN.md        # 본 계획서
```

---

## 3. 핵심 모듈별 상세 설계

### 3.1. 3D 축구장 씬 & 캐릭터 (`scene-manager.js`, `character-model.js`)
* **축구장 배경 구성**:
  * 바닥: 초록색 투톤 스트라이프 잔디 텍스처 메쉬 + 백색 라인(페널티 박스/센터 라인).
  * 후방 오브젝트: 로우폴리 축구 골대(Goalpost) 및 네트 메쉬.
  * 조명: 야간 경기장 스포트라이트 연출(DirectionalLight + AmbientLight).
* **Daisy Bell 스타일 로우폴리곤 마네킹 (Uncanny B급 병맛 감성)**:
  * **비주얼 레퍼런스**: 초기 3D CGI 감성의 각진 로우폴리 안드로이드 마네킹 (Daisy Bell / I Feel Fantastic 모델링).
  * **머리(Head)**: 다면체 로우폴리 메쉬(`IcosahedronGeometry` 또는 각진 다면체 박스). 전면의 평평한 면에 사용자의 실사 얼굴 텍스처를 정면 투영하여 기괴한 입체-평면 부조화 연출.
  * **몸통 및 사지**: 매끄러운 곡선 배제, 각진 6~8각 기둥(`CylinderGeometry(radius, radius, height, 6~8)`) 및 각진 관절 블록의 계층 구조.
  * **셰이딩 및 재질**: `MeshStandardMaterial`에 `flatShading: true`를 강제 적용하여 폴리곤 면마다 각진 명암이 두드러지는 질감 연출.
* **3대 콘셉트 스킨 스왑 (지오메트리 공유, 머티리얼 분할)**:
  1. `CLASSIC_DAISY`: 창백한 회백색 무광 플라스틱 피부(`0xe6dfd5`, `roughness: 0.4`), 원조 Daisy Bell의 기괴하고 미니멀한 안드로이드 감성.
  2. `RETRO_JERSEY`: 상체 몸통에 원색 스트라이프 축구 유니폼 컬러 + 하체 흰색 반바지 및 축구 양말 투톤 분할, 조기축구회 마네킹 감성.
  3. `GOLDEN_TROPHY`: 전신 고광택 메탈릭 골드(`0xffd700`, `metalness: 0.9`, `roughness: 0.15`), 세레모니하는 황금 발롱도르 트로피 감성.
* **대두(Bobblehead) 슬라이더**:
  * 머리 메쉬 스케일 `headMesh.scale.set(s, s, s)` (0.8배 ~ 2.5배 실시간 조절).
* **세레모니 포즈 프리셋**:
  1. `GEOJE_YAHO` (리센느 거제 야호: 양손 입가 확성기 + 젖힌 상체)
  2. `CHOI_SAN_BAD` (최산 BAD: 치명적 어깨 꺾기 + 날카로운 턱선 폼)
  3. `RONALDO_SIU` (호날두 시우: 공중 착지 후 양팔 뒤로 포효)
  4. `CUTE_HEART` (축구장에서 하는 앙증맞은 양손 볼하트)
  5. `KNEEL_SLIDE` (무릎 슬라이딩 포즈)

### 3.2. 얼굴 크롭 툴 (`face-cropper.js`)
* **업로드 검증 (T03-C04, C05, C09, C10)**:
  * PNG/JPEG 허용. 기타 파일(SVG, TXT, EXE 등)은 `"PNG 및 JPEG 파일만 지원합니다."` 거부 메시지 출력 및 기존 작업 보존.
* **크롭 UI**:
  * 타원형 가이드 틀 제공. 마우스 드래그/휠로 사진 위치와 확대율 조절 후 [얼굴 적용] 클릭.
  * 512×512 크롭 텍스처를 머리 전면 슬롯 머티리얼에 주입.

### 3.3. 화면비 제어 & 3D 카메라 무빙 캡처 & 다운로드 일치 (T03-C11~C13)
* **화면비**: 1:1 (`1080×1080`), 4:5 (`1080×1350`), 9:16 (`1080×1920`) 뷰포트 토글.
* **자유로운 3D 카메라 무빙 인터랙션**:
  * **마우스 좌클릭 드래그**: 360도 궤도 회전 (역동적 로우앵글, 하이앵글, 측면 얼짱각도 연출).
  * **마우스 우클릭 / Shift+드래그**: 카메라 위치 상하좌우 패닝(Pan) (캐릭터를 화면 좌측/우측/중앙 원하는 위치에 자유 배치).
  * **마우스 휠 스크롤**: 줌인/줌아웃 (원거리 전신 샷 ~ 극단적 대두 얼빡샷).
  * **원클릭 카메라 앵글 퀵버튼**:
    * `정면 전신 (Front)`
    * `역동적 로우앵글 (Dynamic Low)`
    * `대두 얼빡샷 (Close-up)`
    * `구도 초기화 (Reset)`
* **원하는 구도 그대로 100% 캡처 & 다운로드 파이프라인**:
  * 사용자가 마우스로 돌리고 이동시켜 멈춰둔 그 시점(`camera.position`, `camera.quaternion`, `controls.target`)을 실시간 WebGL 버퍼에 보존(`preserveDrawingBuffer: true`).
  * 다운로드 시 오프스크린 2D 캔버스에 [사용자가 맞춘 3D 카메라 프레임]을 정확히 복제하고, 그 위에 [2D 문구]를 비례 좌표로 얹어 `canvas.toBlob('image/png')`로 추출.
  * 템플릿 저장 시에도 카메라의 3차원 위치와 주시점 타깃을 완벽히 저장하여 불러왔을 때 그 앵글 그대로 복원.

### 3.4. 텍스트 컨트롤 (T03-C06~C08)
* 억지 유행어 프리셋 배제. 순수 폼 컨트롤 방식:
  * 텍스트 내용 인풋
  * 폰트 크기 슬라이더
  * 폰트 색상 피커
  * Y 위치 슬라이더 (상단/중앙/하단)
  * 자동 줄바꿈 알고리즘 적용 (긴 문장 캔버스 밖 이탈 방지)

### 3.5. 템플릿 CRUD 및 JSON 입출력 (T03-C17~C24)
* **데이터 모델**:
  ```json
  {
    "id": "tpl_1725960000000_abc",
    "title": "야호 세레모니",
    "ratio": "9:16",
    "pose": "GEOJE_YAHO",
    "skin": "RETRO_JERSEY",
    "headScale": 1.8,
    "cameraPosition": { "x": 0, "y": 1.5, "z": 4 },
    "cameraTarget": { "x": 0, "y": 1.2, "z": 0 },
    "text": "오늘도 출석 완료",
    "fontSize": 48,
    "fontColor": "#ffffff",
    "textY": 0.85,
    "faceImageDataUrl": "data:image/png;base64,...",
    "updatedAt": "2026-09-10T14:50:00.000Z"
  }
  ```
* **JSON 유효성 검사 3단계**:
  1. 정상 JSON: 파싱 후 템플릿 복원 (T03-C22)
  2. 문법 손상 JSON: 구문 오류 안내 및 기존 목록 유지 (T03-C23)
  3. 필수 누락 JSON: 스키마 검증 실패 안내 및 기존 목록 유지 (T03-C24)

---

## 4. 극단 입력 12건 테스트 명세 (`docs/test-cases.md`)

1. TC-01: 100자 이상 긴 한글 문구 입력 (자동 줄바꿈 검증, FAIL->PASS 개선 사례)
2. TC-02: 영문/특수문자 혼합 입력
3. TC-03: 다중 줄바꿈(`\n\n`) 입력
4. TC-04: 이모지 포함 문구 입력
5. TC-05: 빈 문자열 / 공백 입력
6. TC-06: 극단적 세로 비율 원본 사진 업로드
7. TC-07: 극단적 가로 파노라마 사진 업로드
8. TC-08: 알파 채널 투명 PNG 사진 업로드
9. TC-09: 폰트 최소 크기(12px) 입력
10. TC-10: 폰트 최대 크기(140px) 입력
11. TC-11: 미지원 `.txt` 파일 업로드 (거부 및 기존 상태 유지)
12. TC-12: 문법이 깨진 손상된 JSON 업로드 (거부 및 기존 템플릿 유지)

---

## 5. 보안 및 제출물 작성 규격

* **개인정보/메타데이터 (T03-C28~C30)**:
  * Canvas 재인코딩 출력으로 EXIF 위치 메타데이터 0건.
  * 코드 및 배포물에 비밀값, 개인정보 0건.
* **완성 이미지 3건 (T03-C25~C27)**:
  * 1:1, 4:5, 9:16 비율 각 1건. 본인 제작 표기 또는 출처/라이선스 명기.
* **제출 규격 문서 (`docs/submission.md`)**:
  * 4줄 확인 방법 (어디로/무엇을/통과모습/실패모습)
  * 3줄 AI 판단 (AI에게 맡긴 일/직접 판단한 일/AI 제안 따르지 않은 일)

---

## 6. 새 세션에서의 개발 단계
1. **1단계**: HTML/CSS 기본 레이아웃 구성 (축구장 뷰포트 + 컨트롤 바)
2. **2단계**: Three.js 축구장 배경(잔디/골대/조명) 및 마네킹 캐릭터 구축
3. **3단계**: 포즈 프리셋(거제 야호, 최산 BAD, 시우우) 및 대두 슬라이더 구현
4. **4단계**: 사진 업로드 및 타원형 얼굴 크롭 툴 연동
5. **5단계**: 2D 문구 폼 컨트롤 및 화면비별(1:1, 4:5, 9:16) PNG 다운로드 합성기 구현
6. **6단계**: 템플릿 CRUD(LocalStorage) 및 JSON 3단계 검증기 구현
7. **7단계**: 극단 입력 12건 검사, 완성 이미지 3건, `submission.md` 작성 및 GitHub Pages 배포
